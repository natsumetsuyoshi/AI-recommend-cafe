/**
 * クライアントとサーバーで共有する定数。
 *
 * `server-only` は付けない。ブラウザ側の入力制限とサーバー側の検証で
 * 同じ値を使うために、両方から import できる場所に置く。
 */

/**
 * 気分テキストの最大文字数。
 *
 * サーバーでは検証の上限、ブラウザでは `<textarea maxLength>` に使う。
 * Claude へ渡す入力トークン＝コストを抑えるための上限でもある。
 */
export const MOOD_MAX_LENGTH = 200;
